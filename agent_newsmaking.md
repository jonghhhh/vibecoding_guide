# AI와 함께하는 기사 작성

### 자료를 폴더에 넣으면 핵심 정리 → 기사 → 팩트체크까지

> **대상**: 코딩 경험이 거의 없는 기자<br>
> **도구**: Claude Code (Codex·Antigravity CLI도 파일 이름만 바꾸면 동일)<br>
> **목표**: 자료를 폴더에 넣고 한 마디 시키면 **핵심 정리 → 기사 작성 → 사실 검증**까지 한 번에<br>
> **핵심 원칙**: AI는 보조 기자입니다. **최종 판단과 책임은 사람 기자**에게 있습니다.

---

## 1. 무엇을 만드나

자료 몇 개를 넣고 "기사 만들어줘" 한 마디면 이 순서로 진행됩니다.

```text
sources/ 의 자료
   │
   ▼ 1. 핵심 정리        → output/key_facts.md   (메인 세션이 직접)
   │
   ▼ 2. 검색 보완(선택)   → 웹에서 배경·최신 수치 보완
   │
   ▼ 3. 기사 작성        → output/article.md     (스킬 + 내 문체 + 뉴스가치)
   │
   ▼ 4. 사실 검증        → output/factcheck.md   (서브에이전트, 독립 검증)
```

이 과정에서 심화 가이드의 세 기능을 실제로 써 봅니다.

| 기능 | 하는 일 | 이 실습에서 |
|---|---|---|
| **CLAUDE.md** | 프로젝트 규칙을 매 세션 자동으로 읽힘 | 작업 순서·내 문체·뉴스가치를 한 파일에 |
| **스킬 1개** | 절차를 저장해 두면 해당 단계에서 자동 적용 | 기사 작성 절차 |
| **서브에이전트 1개** | 독립된 문맥에서 따로 일함 | 팩트체크(작성 내용에 휘둘리지 않게) |

> **1·2단계는 왜 파일이 없나요?** 핵심 정리와 검색 보완은 메인 세션(기본 Claude)이 직접 합니다. 파일 읽기와 웹 검색은 기본 기능이라, `CLAUDE.md`에 적어 둔 순서대로 처리됩니다.

---

## 2. 폴더 구조

```text
agent_newsmaking/
├── CLAUDE.md          # 규칙 + 작업 순서 + 뉴스가치 + 내 문체
├── my_style.txt       # 내 기사 샘플 2~3편 (문체 학습용)
├── sources/           # 원자료 (txt 또는 텍스트형 PDF)
├── output/            # 결과물 (핵심 정리·기사·검증)
└── .claude/
    ├── skills/
    │   └── write-article/SKILL.md   # 스킬 1개 (기사 작성)
    └── agents/
        └── fact-checker.md          # 서브에이전트 1개 (검증)
```

우리가 만드는 건 사실상 **네 개**입니다 — `CLAUDE.md`, `my_style.txt`, 스킬 1개, 에이전트 1개. 파이썬 파일도 가상환경도 없습니다.

---

## 3. 준비

### 3-1. Claude Code 실행

아직 설치 전이라면 아래 한 줄이면 됩니다. Node.js도 필요 없습니다.

```bash
# macOS · Linux · WSL
curl -fsSL https://claude.ai/install.sh | bash
```

```powershell
# Windows · PowerShell
irm https://claude.ai/install.ps1 | iex
```

> npm을 선호하면 `npm install -g @anthropic-ai/claude-code`도 됩니다. 자세한 설치·로그인은 [입문 가이드 3부](./vibecoding_basic.html)를 참고하세요.

폴더를 만들고 그 안에서 실행합니다.

```bash
mkdir agent_newsmaking && cd agent_newsmaking
claude
```

> ✅ **체크포인트**: 터미널에 Claude Code 입력창이 뜨고, 경로 끝이 `agent_newsmaking`이면 준비 완료입니다.

### 3-2. 자료 넣기

`sources/` 폴더에 원자료(txt 또는 **텍스트형 PDF**)를 넣습니다. 여러 개 넣어도 모두 읽습니다.

### 3-3. 내 문체 샘플 정리

`my_style.txt` 한 파일에 과거 기사 **2~3편**을 구분선으로 나눠 붙입니다. 한 편보다 2~3편이 좋습니다(리드·문장 길이·인용 처리 같은 패턴이 더 잘 드러납니다). 가능하면 성격이 다른 글을 섞으세요(예: 스트레이트 1편 + 해설/기획 1편).

```text
=== 샘플 기사 1 ===
(기사 전문 붙여넣기)

=== 샘플 기사 2 ===
(기사 전문 붙여넣기)
```

---

## 4. 설정 파일 만들기

아래 세 파일을 만듭니다. **직접 타이핑할 필요는 없습니다** — Claude Code 안에서 "이 문서의 4-1~4-3 내용대로 파일 세 개를 만들어 줘"라고 시키면 됩니다.

### 4-1. `CLAUDE.md`

```markdown
# 뉴스 작성 프로젝트 규칙

## 역할
- 너는 우리 뉴스룸의 보조 기자다. 최종 판단과 책임은 사람 기자에게 있다.
- 모든 답과 기사는 한국어로 쓴다.
- 사실·수치·인용은 지어내지 않는다. 근거가 없으면 "[확인 필요]"로 표시한다.

## 입력/출력
- 원자료: sources/  (txt, 텍스트형 PDF)
- 내 문체 샘플: my_style.txt
- 결과물: output/

## 작업 순서 ("기사 만들어줘"라고 하면 이 순서대로 진행한다)
1. sources/의 모든 파일을 읽고 핵심 사실을 정리한다. 각 사실에 출처(파일명·위치)를 단다. → output/key_facts.md
2. 배경·최신 수치가 더 필요하면 웹에서 검색해 보완하고, 출처 URL을 단다(선택).
3. write-article 스킬로 기사를 쓴다. → output/article.md
4. fact-checker 서브에이전트로 기사를 검증한다. → output/factcheck.md
각 단계 결과는 output/에 파일로 남기고, 단계마다 무엇을 했는지 한 줄로 보고한다.

## 뉴스가치 (기사에서 무엇을 앞세울지)
- 새로움·중요성·사회적 파장·유명성·갈등 등으로 평가해, 가장 가치 높은 사실을 리드와 앵글로 삼는다.
- 가치는 선택과 강조에만 쓰고, 사실 자체는 과장·왜곡하지 않는다.

## 내 문체
- 상세 문체는 my_style.txt의 샘플을 따른다.
- 리드는 한 문장, 핵심 수치를 앞세운다. 문장은 짧게, 번역투를 피한다.

## 금지
- 출처 없는 단정, 가짜 인용, 수치 왜곡(단위·기간 누락), 낚시성 제목.
```

### 4-2. 스킬 — `.claude/skills/write-article/SKILL.md`

```markdown
---
name: write-article
description: 정리된 핵심 사실을 바탕으로 내 문체로 기사를 쓸 때 사용한다.
---
# 기사 작성 절차
1. output/key_facts.md(와 검색 보완 내용)만 근거로 쓴다. 근거 없는 문장은 쓰지 않는다.
2. 시작 전 my_style.txt를 읽어 리드 방식·문장 길이·어휘·인용 패턴을 파악한다.
3. 뉴스가치(새로움·중요성·사회적 파장·유명성·갈등)로 사실을 평가해, 가장 가치 높은 것을 리드와 앵글로 삼는다. 단 과장·왜곡은 금지.
4. 구조: 리드(핵심 한 문장) → 핵심 사실 → 인용 → 배경 → 전망.
5. 해석은 [추론]으로 표시한다.
6. output/article.md 로 저장한다(기본 800자).
```

### 4-3. 서브에이전트 — `.claude/agents/fact-checker.md`

```markdown
---
name: fact-checker
description: 작성된 기사의 사실·수치·인용을 핵심 사실 정리와 원문에 대조해 검증할 때 사용.
tools: Read, Grep, WebSearch, WebFetch
model: opus
---
너는 깐깐한 팩트체크 데스크다. 기사의 검증 가능한 모든 문장을 하나씩 점검한다.
각 문장의 근거를 output/key_facts.md와 원문 sources/에서 찾아 매칭하고 판정한다.
판정: [확인] / [출처 불일치] / [근거 없음] / [과장].
인용은 원문과 글자 단위로, 수치는 단위·기간을 대조한다.
문제 항목은 무엇이 틀렸는지와 수정안을 함께 제시한다.
결과는 '문장 / 판정 / 근거 / 수정안' 표로 내고, 통과 못한 항목을 먼저 경고한다.
고치지 말고 지적만 한다.
```

> **`tools` 줄에 Edit·Write가 없는 것이 핵심입니다.** 검증자에게 고칠 권한을 주지 않아, **작성과 검증을 분리**합니다. 스스로 쓴 글을 스스로 통과시키는 일을 막습니다.

> ✅ **체크포인트**: `/agents`를 입력했을 때 목록에 `fact-checker`가 보이면 제대로 인식된 것입니다.

---

## 5. 실행

준비가 끝나면 한 마디면 됩니다.

```text
기사 만들어줘. CLAUDE.md의 작업 순서대로 sources를 읽고 핵심 정리 → 기사 작성 → 검증까지 진행하고, 단계마다 한 줄로 보고해.
```

> ✅ **체크포인트**: `output/` 폴더에 `key_facts.md`·`article.md`·`factcheck.md` 세 파일이 생기면 성공입니다.

기능을 하나씩 체감하고 싶을 때는 단계를 끊어서 시킵니다.

```text
sources 폴더의 모든 자료를 읽고 핵심 사실만 정리해서 output/key_facts.md 로 저장해.
```

```text
방금 정리한 사실로 write-article 스킬을 써서 기사를 써 줘.
```

```text
fact-checker 서브에이전트로 기사를 검증해 줘.
```

> **큰 작업 전에는 `Shift+Tab`으로 플랜 모드**에 들어가세요. 어떤 스킬·서브에이전트가 언제 작동하는지 계획으로 먼저 보여 주어 교육용으로 좋습니다.

---

## 6. PDF에 대해

**텍스트형 PDF**(복사·검색이 되는 일반 보도자료·리포트)와 txt는 Claude Code가 그대로 읽습니다. 강의 전에 샘플 PDF 한두 개를 열어 **텍스트가 제대로 잡히는지** 확인해 두면 안전합니다.

**스캔본(이미지 PDF)**이나 표·수치가 빽빽한 공시는 그대로는 빠지거나 어긋날 수 있습니다. 이런 자료가 많으면 먼저 마크다운으로 변환하는 단계를 앞에 붙이세요.

```text
sources 폴더의 스캔 PDF를 먼저 마크다운으로 변환해서 sources/md/ 에 저장한 뒤, 그걸 원자료로 써 줘.
```

---

## 7. 다른 도구로 할 때

Codex나 Antigravity CLI를 쓴다면 **파일 이름과 호출 방법만** 바뀝니다. 내용은 그대로 옮기면 됩니다.

| | Claude Code | OpenAI Codex | Antigravity CLI |
|---|---|---|---|
| 규칙 파일 | `CLAUDE.md` | `AGENTS.md` | `AGENTS.md` |
| 스킬 | `.claude/skills/<이름>/SKILL.md` | `$이름`으로 호출 | `.agents/skills/<이름>/SKILL.md` |
| 서브에이전트 | `.claude/agents/<이름>.md` | `.codex/agents/<이름>.toml` | `/agents` |
| 계획 먼저 보기 | `Shift+Tab` | `/plan` → `/exec` | `/permissions`로 승인 설정 |

> 변환도 맡기면 됩니다 — "`.claude/` 설정을 Codex용으로 변환해 줘"라고 하면 한 번에 처리됩니다. 자세한 대응표는 [심화 가이드 부록](./vibecoding_advanced.html#codex)에 있습니다.

---

## 8. 잊지 말 것

**검색 보완**은 Claude Code 내장 웹 검색으로 충분합니다. 더 정밀한 한국어 출처나 정확한 수치가 중요해지면 검색 MCP(Tavily·Brave 등)를 **하나만** 붙이세요. 여러 개는 오히려 혼란스럽습니다.

**윤리 두 가지**를 꼭 지킵니다.

1. **AI 보조로 작성했다는 사실을 밝힙니다.**
2. **최종 인용과 수치는 사람 기자가 1차 출처로 다시 확인합니다.**

AI는 초안과 검증을 돕는 도구이며, **출고 책임은 사람에게** 있습니다. 팩트체크 결과가 "전부 [확인]"으로 나와도 그것은 *제출한 자료와 일치한다*는 뜻일 뿐, *자료 자체가 옳다*는 보증은 아닙니다.

> 도구 명령·기본 모델은 2026년 8월 기준이며 자주 바뀝니다. 막히면 Claude Code에서 `/help`를 먼저 확인하세요.
