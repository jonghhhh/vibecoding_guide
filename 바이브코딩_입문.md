# <바이브 코딩 입문> 가이드

### 코딩을 몰라도 됩니다 — 말로 시켜서 프로그램 만들기

> **대상**: 코딩을 거의 해본 적 없는 완전 초보<br>
> **준비물**: 노트북(Windows / macOS), 인터넷, 도구 계정 하나<br>
> **목표**: 코드를 외워서 타이핑하는 방식에서 벗어나, **AI에게 우리말로 시켜 프로그램을 만드는** 경험을 직접 해보기<br>
> **기준일**: 2026년 8월 — 도구 버전·모델명·요금은 자주 바뀝니다. 마지막 절 참고문헌에서 최신 정보를 확인하세요.

---

## 0부. 5분 오리엔테이션

### 0-1. 바이브 코딩이 뭔가요?

> **바이브 코딩(Vibe Coding)** = 프로그래밍 언어 대신 **우리말로 AI에게 시켜서** 프로그램을 만드는 방식입니다.

- `for` 문을 외울 필요가 없습니다. **"할 일을 추가하고 지울 수 있는 웹페이지 만들어 줘"** 라고 말하면 AI가 코드를 씁니다.
- 우리가 할 일은 결과를 보고 **"이렇게 고쳐 줘"** 라고 대화하면서 완성도를 높이는 것입니다.
- 즉, **나는 감독, AI는 배우**입니다.

### 0-2. 오늘 뭘 하게 되나요? — 전체 지도

```text
① 준비물 설치        VS Code + 터미널 + Node.js + 파이썬        (1부, 약 20분)
        ↓
② 도구 하나 설치      Claude Code / Codex / Antigravity 중 택 1  (3~5부, 약 5분)
        ↓
③ 첫 결과물          "오늘의 할 일" 웹앱을 우리말로 주문         (약 10분)
        ↓
④ 대화로 수정        "다크 모드 버튼 추가해 줘"                  (약 10분)
        ↓
⑤ 규칙 심어두기      CLAUDE.md / AGENTS.md 파일 하나            (약 5분)
```

**세 도구를 다 설치할 필요는 없습니다.** 사용법이 거의 같으므로, 계정이 있는 것 **하나만** 골라서 끝까지 진행하면 됩니다.

### 0-3. 꼭 기억할 3가지

1. **나는 감독, AI는 배우** — 코드를 한 줄씩 쓰는 사람이 아니라, 무엇을 만들지 **정하고 검수하는** 사람입니다.
2. **작게, 한 번에 한 단계** — "쇼핑몰 만들어 줘"보다 "할 일을 입력하는 칸 하나 만들어 줘"가 훨씬 잘 됩니다.
3. **AI는 가끔 틀립니다** — 그럴듯한데 틀린 답을 내놓습니다(할루시네이션). **반드시 실행해 보고 확인**하세요.

### 0-4. 셋 중 무엇을 고를까요?

| 도구 | 만든 곳 | 실행 명령 | 이럴 때 고르세요 |
|---|---|---|---|
| **Claude Code** | Anthropic | `claude` | 입문 자료가 가장 많고 코딩 품질 평이 좋습니다. **첫 도구로 추천** |
| **OpenAI Codex** | OpenAI | `codex` | 이미 ChatGPT를 결제 중이라면 추가 비용 없이 시작 |
| **Antigravity CLI** | Google | `agy` | 구 Gemini CLI의 후속. 검색 근거가 필요한 작업에 강함 |

> **세 도구 모두 "에이전트형 CLI"입니다** — 코드를 추천만 하는 게 아니라 **파일을 직접 만들고 고치고 명령어까지 실행**합니다. 그래서 항상 "실행해도 될까요?"라고 묻고, 사람이 내용을 보고 승인하는 절차를 거칩니다.

> **참고**: 세 도구 모두 유료 구독이 사실상 필요합니다. Antigravity의 무료 등급은 **하루 20회 안팎**이라 실습 도중 막히기 쉽습니다(5-5절 참고).

---

## 1부. 준비물 설치 — 모든 도구의 출발선

세 도구 모두 **① VS Code(편집기) + ② 터미널 + ③ Node.js + ④ 파이썬** 위에서 돌아갑니다. 이 넷만 깔면 나머지는 거의 같습니다.

> **용어가 낯설면** 부록 B의 [용어 사전](#b)을 먼저 훑어보세요.

### 1-1. VS Code 설치 (글 쓰는 창)

1. 브라우저에서 **`code.visualstudio.com`** 접속 → **Download** 버튼.
2. 내려받은 파일을 실행해 설치합니다.
3. 메뉴를 한글로 보고 싶다면, 실행 후 왼쪽 확장(Extensions) 탭에서 `Korean Language Pack`을 설치합니다.

> ✅ **체크포인트**: VS Code가 열리고 왼쪽에 아이콘 줄이 보이면 성공입니다.

### 1-2. 터미널 열기 (AI에게 말 거는 창구)

- VS Code 상단 메뉴 → **Terminal → New Terminal**. 단축키는 <code>Ctrl</code> + 백틱(<code>`</code>)입니다.
- 화면 아래쪽에 까만 입력창이 나타나면 성공입니다. 앞으로 **여기에 명령어를 입력**합니다.

> ✅ **체크포인트**: 까만 창에 `PS C:\...>`(Windows) 또는 `사용자이름@맥북 ~ %`(macOS) 같은 글자가 보이면 준비 완료입니다.

> **터미널이 무섭게 생겼나요?** 그냥 **명령어를 받아 적는 메모장**이라고 생각하세요. 이 문서의 명령어는 모두 **복사해서 붙여넣고 Enter**만 누르면 됩니다. 코드 블록 오른쪽 위의 **복사** 버튼을 쓰세요.

### 1-3. Node.js 설치 (엔진)

Claude Code와 Codex를 **npm으로 설치할 때** 필요합니다. (설치 스크립트 방식을 쓰면 없어도 되지만, 깔아 두면 두루 편합니다.)

1. **`nodejs.org`** 접속 → **LTS 버전** 다운로드·설치. Windows는 설치 중 "Add to PATH" 체크를 그대로 두세요.
2. 설치가 끝나면 **터미널을 껐다 켜고** 아래를 입력합니다.

```bash
node -v
npm -v
```

> ✅ **체크포인트**: `v22.x.x` 처럼 버전 번호가 나오면 성공입니다.

> **macOS**에서 Homebrew를 쓴다면 `brew install node` 한 줄로 끝납니다.
> **Antigravity CLI**는 단일 실행파일이라 Node.js 없이도 설치됩니다(5부 참고).

### 1-4. 파이썬(Python) 설치

바이브 코딩으로 만든 프로그램을 실행하거나 데이터를 다루려면 파이썬이 필요합니다.

1. 브라우저에서 **`python.org`** 접속 → 상단 **Downloads**.
2. 내 컴퓨터에 맞는 버전을 내려받습니다.
   - **Windows**: 노란 버튼 "Download Python 3.x.x" 클릭. 설치 첫 화면에서 **"Add python.exe to PATH" 체크가 필수**입니다. 체크한 뒤 "Install Now".
   - **macOS**: "macOS 64-bit universal2 installer"를 내려받아 실행합니다.
3. 설치 확인 — 터미널에 입력합니다.

```bash
python --version
```

- macOS에서는 `python3 --version`으로 확인합니다.
- 권장 버전은 **3.11 이상**입니다.

> ✅ **체크포인트**: `Python 3.13.x` 처럼 버전이 보이면 성공입니다.

> **"Add to PATH"를 빠뜨렸다면** — 터미널이 `python`을 못 찾습니다. 설치 파일을 다시 실행해 **Modify → PATH 추가**를 선택하세요.

### 1-5. 작업 폴더 만들기 (집 짓기 전 땅 고르기)

원하는 위치에 빈 폴더를 만들고 VS Code에서 **File → Open Folder**로 엽니다. 터미널에서 직접 만들어도 됩니다.

```bash
mkdir my-first-vibe
cd my-first-vibe
```

> ✅ **체크포인트**: VS Code 왼쪽 탐색기에 `MY-FIRST-VIBE` 폴더 이름이 보이고, 터미널 경로 끝이 `my-first-vibe`이면 성공입니다.

> **왜 폴더가 중요한가요?** AI 코딩 도구는 **터미널이 현재 열고 있는 폴더**를 작업 공간으로 삼습니다. 엉뚱한 폴더에서 실행하면 엉뚱한 곳에 파일이 생깁니다. 실행 전 항상 폴더를 확인하세요.

### 1-6. 오늘의 공통 과제

> **"오늘의 할 일(To-Do) 웹앱"**
> 할 일을 입력해 추가하고, 완료하면 체크하고, 삭제할 수 있는 한 페이지짜리 웹앱. 별도 설치 없이 브라우저로 바로 열 수 있도록 **HTML 파일 하나**로 만듭니다.

3·4·5부에서 **같은 과제를 도구만 바꿔** 만들어 봅니다. 결과물이 어떻게 다른지 비교하는 것이 목적입니다.

---

## 2부. 가상환경(.venv)과 pip — 파이썬을 쓸 때만 {#venv}

> **급하면 건너뛰어도 됩니다.** 3부(첫 실습)를 먼저 해보고, 파이썬 프로그램을 만들 때 이 절로 돌아오세요. 다만 이 절만 익혀 두면 **설치 관련 사고가 거의 사라집니다.**

### 2-1. 왜 가상환경을 쓰나요?

- **문제**: 패키지를 컴퓨터 전체에 설치하면 프로젝트끼리 버전이 충돌합니다. A 프로젝트는 requests 2.28이 필요한데 B 프로젝트가 2.32로 덮어쓰는 식입니다.
- **비유**: 가상환경은 **프로젝트 전용 서랍**입니다. 서랍 안에만 물건을 넣으니 다른 프로젝트와 섞이지 않습니다. 프로젝트를 지울 땐 서랍째 버리면 끝입니다.
- **결과**: "내 컴퓨터에선 되는데" 문제가 크게 줄어듭니다.
- **이 강의의 표준**: 파이썬 기본 기능인 `venv` + `pip`. 추가로 배울 도구가 없습니다.

> **이름이 왜 `.venv`인가요?** 관례입니다. 점(.)으로 시작해 숨김 폴더가 되고, VS Code·깃 등 대부분의 도구가 이 이름을 자동으로 알아봅니다.

### 2-2. 만들기 · 켜기 · 끄기

**① 작업 폴더로 이동** — 가상환경은 프로젝트 폴더 안에 만듭니다.

```bash
cd my-first-vibe
```

**② 가상환경 만들기** — 폴더 안에 `.venv/`가 생깁니다. **한 프로젝트에 한 번만** 실행하면 됩니다.

```bash
# Windows
python -m venv .venv

# macOS / Linux
python3 -m venv .venv
```

**③ 활성화(켜기)** — 터미널을 새로 열 때마다 필요합니다.

```bash
# Windows · PowerShell (VS Code 기본 터미널)
.venv\Scripts\Activate.ps1

# Windows · 명령 프롬프트(cmd)
.venv\Scripts\activate.bat

# macOS / Linux
source .venv/bin/activate
```

**④ 켜졌는지 확인** — 터미널 입력줄 맨 앞에 `(.venv)`가 붙으면 성공입니다.

```text
(.venv) PS C:\vscode\my-first-vibe>
 ↑ 이 표시가 핵심입니다
```

**⑤ 끄기** — 다른 프로젝트로 옮길 때.

```bash
deactivate
```

> ✅ **체크포인트**: 줄 맨 앞에 `(.venv)`가 보이나요? 안 보이면 아직 켜지지 않은 것입니다.

### 2-3. pip로 패키지 설치하기

**반드시 `(.venv)`가 켜진 상태에서 설치하세요.** 그래야 서랍 안으로 들어갑니다.

```bash
pip install requests python-dotenv
```

| 명령 | 하는 일 |
|---|---|
| `pip install 패키지명` | 패키지 설치 |
| `pip list` | 지금 서랍에 뭐가 들었는지 확인 |
| `pip freeze > requirements.txt` | 설치 목록을 파일로 저장(버전까지 고정) |
| `pip install -r requirements.txt` | 저장된 목록 그대로 재설치. 다른 컴퓨터에서 똑같은 환경 복원 |
| `pip install --upgrade 패키지명` | 최신 버전으로 올리기 |

**실행** — 활성화 상태에서 그냥 `python`을 씁니다.

```bash
python wordcount.py
```

> **공유·백업 규칙** — `.venv/` 폴더는 **공유하지 않습니다**(용량이 크고 OS마다 내용이 다릅니다). 대신 `requirements.txt`만 공유하세요. `.gitignore`에 `.venv/`와 `.env`를 넣어 두는 것도 잊지 마세요.

### 2-4. VS Code에 가상환경 연결하기

VS Code가 어느 파이썬을 쓸지 알려 줘야 자동완성·오류 표시가 맞게 동작합니다.

1. VS Code 확장 탭에서 **Python** 확장(제작사 Microsoft)을 설치합니다.
2. `Ctrl+Shift+P`(macOS는 `Cmd+Shift+P`) → **Python: Select Interpreter** 입력.
3. 목록에서 `.venv`가 붙은 항목을 고릅니다. 보통 `('.venv': venv)`로 표시됩니다.
4. 이후 VS Code 터미널을 새로 열면 `(.venv)`가 자동으로 켜집니다.

### 2-5. 가상환경에서 자주 나는 오류

| 증상 | 원인과 해결 |
|---|---|
| PowerShell에서 `이 시스템에서 스크립트를 실행할 수 없으므로…` | Windows 스크립트 실행 정책 때문입니다. 아래 명령을 한 번 실행한 뒤 다시 활성화하세요 |
| `ModuleNotFoundError: No module named 'requests'` | 설치는 했는데 **활성화를 안 한 상태**로 실행한 경우가 대부분입니다. `(.venv)` 표시부터 확인 |
| `pip: command not found` | 파이썬 미설치 또는 PATH 누락. `python -m pip install ...`로 우회할 수 있습니다 |
| 설치했는데 VS Code가 빨간 줄로 표시 | 인터프리터 미선택. 2-4절을 다시 수행 |
| 폴더를 옮겼더니 활성화가 안 됨 | `.venv`는 경로를 기억합니다. `.venv` 폴더를 지우고 2-2부터 다시 실행 |

```powershell
Set-ExecutionPolicy -Scope CurrentUser RemoteSigned
```

> **막히면 AI에게 그대로 물어보세요** — 오류 메시지를 복사해 붙이고 "내 OS는 Windows야. 이 오류 원인과 해결 방법을 알려줘"라고 요청하면 됩니다. 설치 문제는 AI가 가장 잘 해결하는 영역입니다.

---

## 3부. Claude Code 실습 — 가장 쉬운 출발점

> 이 부분은 조태호 저 **『혼자 공부하는 바이브코딩 with 클로드코드』**(한빛미디어)를 참조했습니다. 더 깊이 배우려면 책과 공식 깃허브(참고문헌 [1])를 추천합니다.

### 3-1. Claude Code란?

- Anthropic이 만든 **터미널 기반 AI 코딩 에이전트**입니다.
- 2026년 8월 기준 기본 모델은 **Claude Opus 5**입니다. 더 빠른 작업엔 **Sonnet 5**, 가벼운 작업엔 **Haiku 4.5**를 고를 수 있습니다.
- 모델 변경은 `/model`, 생각의 깊이 조절은 `/effort`입니다.
- **계정 주의**: 무료 claude.ai 요금제로는 쓸 수 없습니다. Pro(약 $20) 이상 구독 또는 API 계정이 필요합니다.

### 3-2. 설치 — 두 가지 방법 중 택 1

**어느 쪽을 고를까요?**

| | **방법 A. 설치 스크립트 (권장)** | **방법 B. npm** |
|---|---|---|
| Node.js 필요 | ❌ 필요 없음 | ✅ 필요(v22 이상) |
| 자동 업데이트 | ✅ 자동 | 수동(`npm install -g ...@latest`) |
| 이럴 때 | **대부분의 경우 이쪽** | 이미 Node.js를 쓰고 있고 npm에 익숙할 때 |

**방법 A — 설치 스크립트 (curl / irm)**

내 컴퓨터와 터미널 종류에 맞는 **한 줄만** 복사해 실행하세요.

```bash
# macOS · Linux · WSL
curl -fsSL https://claude.ai/install.sh | bash
```

```powershell
# Windows · PowerShell  (창 왼쪽에 PS 가 보이면 PowerShell)
irm https://claude.ai/install.ps1 | iex
```

```batch
REM Windows · 명령 프롬프트(CMD)  (PS 없이 C:\ 로 시작하면 CMD)
curl -fsSL https://claude.ai/install.cmd -o install.cmd && install.cmd && del install.cmd
```

> **PowerShell인지 CMD인지 구분하는 법** — 입력줄이 `PS C:\Users\이름>`이면 **PowerShell**, `C:\Users\이름>`이면 **CMD**입니다. `'irm' is not recognized` 오류가 나면 CMD에서 PowerShell 명령을 쓴 것이고, `'&&' is not a valid statement separator` 오류가 나면 그 반대입니다.

**방법 B — npm**

```bash
npm install -g @anthropic-ai/claude-code
```

> `sudo npm install -g`는 쓰지 마세요. 권한 문제와 보안 위험이 생깁니다.

**그 밖의 방법** — macOS는 `brew install --cask claude-code`, Windows는 `winget install Anthropic.ClaudeCode`도 됩니다. 다만 이 둘은 자동 업데이트가 되지 않습니다.

**설치 확인**

```bash
claude --version
claude doctor
```

> ✅ **체크포인트**: `2.1.x (Claude Code)` 처럼 버전이 보이면 성공입니다. `command not found`가 나오면 **터미널을 완전히 껐다 켠 뒤** 다시 시도하세요.

### 3-3. 실행과 로그인

작업 폴더 안에서 한 단어만 입력합니다.

```bash
claude
```

- 처음 실행하면 브라우저가 열리며 **Anthropic 계정 로그인**을 요청합니다.
- 구독은 Pro 약 $20 / Max $100·$200입니다. 사용량에 따라 고르세요.

> ✅ **체크포인트**: 터미널에 Claude Code 환영 화면과 입력창이 뜨면 성공입니다.

> **VS Code 안에서 쓰기** — 확장 탭에서 **"Claude Code for VS Code"**(제작사 anthropic)를 설치하면, 사이드 패널에서 인라인 diff(바뀐 부분 미리보기), `@파일명` 멘션, `/model`·`/context` 같은 슬래시 명령을 화면으로 쓸 수 있습니다.

### 3-4. 첫 실습 — 할 일 웹앱 만들기

`claude`가 실행된 상태에서 **우리말로** 그대로 입력합니다.

```text
초보자도 이해하기 쉽게, 할 일을 추가·완료체크·삭제할 수 있는 '오늘의 할 일' 웹앱을 만들어 줘.
조건:
1) 설치 없이 브라우저로 바로 열리도록 index.html 파일 하나로 만들 것
2) 디자인은 깔끔하고 모바일에서도 보기 좋게
3) 할 일 목록은 새로고침해도 남아 있도록 브라우저에 저장
```

Claude Code는 **계획을 먼저 보여주고**, 동의하면 `index.html`을 만듭니다. 파일이 생기면 더블클릭하거나 아래 명령으로 엽니다.

```bash
# macOS
open index.html
# Windows
start index.html
```

> ✅ **체크포인트**: 브라우저에 할 일 입력창과 "추가" 버튼이 보이고, 실제로 항목이 추가되면 **첫 바이브 코딩 성공**입니다.

> 아래는 나올 결과물의 **뼈대 예시**입니다. 이런 코드가 나오는지 눈으로 확인하는 용도이며, 외울 필요는 전혀 없습니다.

```html
<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>오늘의 할 일</title>
  <style>
    body { font-family: sans-serif; max-width: 480px; margin: 40px auto; padding: 0 16px; }
    .done { text-decoration: line-through; color: #aaa; }
    li { display: flex; justify-content: space-between; padding: 8px 0; }
  </style>
</head>
<body>
  <h1>오늘의 할 일</h1>
  <input id="todoInput" placeholder="할 일을 입력하세요" />
  <button onclick="addTodo()">추가</button>
  <ul id="todoList"></ul>
  <script>
    // 브라우저 저장소(localStorage)에서 목록 불러오기
    let todos = JSON.parse(localStorage.getItem('todos') || '[]');
    function save() { localStorage.setItem('todos', JSON.stringify(todos)); render(); }
    function addTodo() {
      const v = document.getElementById('todoInput').value.trim();
      if (!v) return;
      todos.push({ text: v, done: false });
      document.getElementById('todoInput').value = '';
      save();
    }
    function render() {
      const ul = document.getElementById('todoList');
      ul.innerHTML = '';
      todos.forEach((t, i) => {
        const li = document.createElement('li');
        li.innerHTML = `<span class="${t.done ? 'done' : ''}"
          onclick="toggle(${i})">${t.text}</span>
          <button onclick="remove(${i})">삭제</button>`;
        ul.appendChild(li);
      });
    }
    function toggle(i) { todos[i].done = !todos[i].done; save(); }
    function remove(i) { todos.splice(i, 1); save(); }
    render();
  </script>
</body>
</html>
```

### 3-5. 대화로 고치기 (바이브 코딩의 핵심)

결과를 보고 **말로** 수정을 요청합니다. 한 번에 하나씩 시키는 것이 요령입니다.

```text
완료한 항목이 자동으로 목록 맨 아래로 내려가게 해 줘. 그리고 상단에 '남은 할 일 N개'를 표시해 줘.
```

```text
다크 모드 켜고 끄는 버튼을 오른쪽 위에 추가해 줘.
```

> ✅ **체크포인트**: 브라우저를 새로고침했을 때 요청한 변화가 보이면 성공입니다. 안 보이면 "적용이 안 된 것 같아. 브라우저에서 강제 새로고침해도 그대로야"라고 그대로 말해 주세요.

### 3-6. 두 번째 실습 — 파이썬 프로그램 만들고 실행하기

웹페이지(HTML)는 브라우저로 열어 봤습니다. 이번에는 **터미널에서 돌아가는 작은 파이썬 프로그램**을 만들어, AI가 코드를 쓰고 직접 실행해 결과까지 보여주는 경험을 합니다.

**먼저 가상환경을 켭니다** — [2부](#venv)대로 `(.venv)`가 켜진 상태에서 진행하세요.

`claude` 실행 상태에서 우리말로 요청합니다.

```text
글을 분석해 글자 수·단어 수·문장 수를 세어 주는 간단한 파이썬 프로그램을 만들어 줘.
조건:
1) 파일 이름은 wordcount.py
2) 분석할 예시 글을 코드 안에 넣어 두고, 결과를 보기 좋게 출력
3) 초보가 읽기 쉽게 한국어 주석을 충분히 달 것
4) 표준 라이브러리만 사용(추가 설치 없이 실행되게)
그리고 그 프로그램을 실행해서 결과까지 보여줘.
```

Claude가 `wordcount.py`를 만들고 **직접 실행해 결과를 보여줍니다.** 만들어질 코드의 예시입니다.

```python
# 분석할 글 (이 부분만 바꾸면 다른 글도 분석됩니다)
text = "오늘 시청에서 기자회견이 열렸다. 시장은 내년 예산을 발표했다. 질문도 이어졌다."

# 글자 수 (공백 포함 / 공백 제외)
char_with_space = len(text)
char_no_space = len(text.replace(" ", ""))

# 단어 수 (공백 기준으로 나눠 셉니다)
word_count = len(text.split())

# 문장 수 (마침표·물음표·느낌표 개수로 단순 계산)
sentence_count = text.count(".") + text.count("?") + text.count("!")

print("글자 수(공백 포함):", char_with_space)
print("글자 수(공백 제외):", char_no_space)
print("단어 수:", word_count)
print("문장 수:", sentence_count)
```

직접 실행해 보려면 터미널에 입력합니다.

```bash
python wordcount.py
```

> ✅ **체크포인트**: 터미널에 `글자 수(공백 포함): 43` 처럼 숫자가 출력되면 성공입니다.

> **외부 패키지가 필요한 프로그램이라면** — `(.venv)`가 켜진 상태에서 `pip install`로 먼저 설치하세요. AI에게 "필요한 패키지를 `pip install`로 설치하고 `requirements.txt`에도 추가해 줘"라고 요청하면 한 번에 처리됩니다.

웹앱 때와 똑같이, 결과를 보고 **말로 고치면** 됩니다.

```text
평균 문장 길이(공백 제외 글자 수 ÷ 문장 수)도 함께 출력해 줘.
```

HTML(웹페이지)과 파이썬(프로그램) 두 가지를 만들어 보면, 바이브 코딩이 웹뿐 아니라 **일반 프로그램에도 똑같이 통한다**는 걸 알 수 있습니다.

### 3-7. CLAUDE.md — 프로젝트에 규칙 심기

폴더에 `CLAUDE.md` 파일을 두면 Claude가 **매번 그 규칙을 자동으로 읽습니다.** 초보에게 특히 강력한 기능입니다.

```markdown
# 프로젝트 규칙
- 모든 설명과 주석은 한국어로 작성한다.
- 초보자가 읽기 쉽게 변수명은 명확하게 짓는다.
- 외부 라이브러리는 꼭 필요할 때만 쓴다.
- 파이썬 패키지는 .venv 가상환경에 pip로 설치한다. 설치 후 requirements.txt를 갱신한다.
```

> 파일을 직접 쓰기 어렵다면 `claude` 안에서 **`/init`** 을 입력하세요. 폴더를 분석해 초안을 자동으로 만들어 줍니다.

> **3부 요약**: 설치 → `claude` 실행 → 우리말로 요청 → 결과 확인 → 대화로 수정 → `CLAUDE.md`로 규칙화. 웹앱이든 파이썬 프로그램이든 같은 사이클이며, 이것이 바이브 코딩의 전부입니다.

---

## 4부. OpenAI Codex 실습 — 같은 앱, 다른 도구

### 4-1. Codex란?

- OpenAI의 **에이전트형 코딩 시스템**입니다. 터미널 CLI·IDE 확장·클라우드·깃허브 봇이 **하나의 계정, 하나의 모델**로 연결됩니다.
- 2026년 8월 기준 모델은 **GPT-5.6 계열**입니다. `/model`로 고를 수 있습니다.
  - **GPT-5.6 Sol** — 가장 강력. 복잡한 코딩·리서치용
  - **GPT-5.6 Terra** — 일상 작업용 균형형(기본으로 무난)
  - **GPT-5.6 Luna** — 가장 빠르고 저렴
- 예전 자료에 나오는 `gpt-5.3-codex`·`gpt-5.2`는 ChatGPT 계정 기준으로 이미 사용이 중단됐고, `gpt-5.4`는 **2026년 8월 31일에 종료**됩니다.

### 4-2. 설치 — 두 가지 방법 중 택 1

**방법 A — 설치 스크립트 (curl / irm)**

```bash
# macOS · Linux
curl -fsSL https://chatgpt.com/codex/install.sh | sh
```

```powershell
# Windows
powershell -ExecutionPolicy ByPass -c "irm https://chatgpt.com/codex/install.ps1 | iex"
```

**방법 B — npm**

```bash
npm install -g @openai/codex
```

> **패키지 이름 주의** — 반드시 `@openai/codex`입니다. 앞의 `@openai/`를 빼고 `codex`만 설치하면 2012년의 전혀 다른 패키지가 깔립니다.

**그 밖의 방법** — macOS는 `brew install --cask codex`도 됩니다.

**설치 확인과 실행**

```bash
codex --version
codex
```

- 처음 실행하면 **ChatGPT 계정으로 로그인**합니다.
- 요금: ChatGPT Plus $20 / Pro $200. 사용량 한도가 다릅니다.

> ✅ **체크포인트**: 터미널에 Codex 입력창이 뜨면 성공입니다.

> **VS Code 안에서 쓰기** — 확장 탭에서 **"OpenAI Codex"** 확장을 설치하면 사이드 패널 에이전트가 열려 있는 파일을 맥락으로 활용합니다. CLI와 상태를 공유합니다.

### 4-3. Claude Code와 다른 점

| | Claude Code | OpenAI Codex |
|---|---|---|
| 규칙 파일 | `CLAUDE.md` | **`AGENTS.md`** |
| 계획 먼저 보기 | `Shift+Tab`(플랜 모드) | **`/plan` → `/exec`** |
| 바뀐 파일 확인 | 인라인 diff | **`/diff`** |
| 권한 | 매번 승인 프롬프트 | **샌드박스 모드를 미리 지정** |
| 현재 상태 | `/context` | `/status` |

- **구조화 명령어**: `/plan`(계획) → `/exec`(실행) → `/review`(검토) 흐름으로 작업을 끊어서 지시할 수 있습니다.
- **주의점**: 변경 전 diff(미리보기)가 약하고 승인 흐름이 거칠다는 평이 있습니다. **바뀐 파일을 꼭 직접 확인**하세요.
- 최근 버전에는 작업 폴더를 옮기는 `/cd`·`/pwd`, 대화를 마크다운으로 내보내는 `/export`, 예상 비용을 보여주는 `/status` 등이 추가됐습니다.

### 4-4. 같은 과제 실습

```text
/plan
할 일을 추가·완료체크·삭제할 수 있는 '오늘의 할 일' 웹앱을
index.html 한 파일로 만들고 싶어. 브라우저 저장 기능도 포함해 줘.
```

계획이 마음에 들면 실행합니다.

```text
/exec
```

작업이 끝나면 무엇이 바뀌었는지 확인하고 다듬습니다.

```text
/diff
```

```text
/review
방금 만든 코드에서 초보가 헷갈릴 만한 부분에 한국어 주석을 더 달아 줘.
```

> **비교 포인트(강의용 질문)**: "Claude Code와 결과물이 어떻게 다른가? 어느 쪽 설명이 더 이해하기 쉬운가?" — 정답은 없습니다. **도구마다 결과가 다르다**는 걸 체감하는 것이 목적입니다.

---

## 5부. 구글 도구 실습 — Antigravity CLI

### 5-1. Gemini CLI는 종료됐습니다 (중요)

> **2026년 6월 18일부로 기존 `gemini` 명령은 종료됐습니다.** 개인용·Google AI Pro·Ultra 요금제에서 **Gemini CLI와 Gemini Code Assist IDE 확장의 요청 처리가 중단**됐고, 이후 `gemini` 호출은 `HTTP 410`을 돌려줍니다. 후속 도구는 **Antigravity CLI**이며 실행 명령은 **`agy`**입니다. (기업용 Gemini Code Assist 라이선스를 보유한 조직은 예외적으로 기존 CLI를 계속 쓸 수 있습니다.)

| 항목 | 구 Gemini CLI | **Antigravity CLI** |
|---|---|---|
| 실행 명령 | `gemini` (종료) | **`agy`** |
| 배포 방식 | npm 패키지(오픈소스) | **설치 스크립트 + 단일 실행파일**(Go, 비공개 소스). Node.js 불필요 |
| 규칙 파일 | `GEMINI.md` | `AGENTS.md` (또는 `.agents/rules/*.md`) |
| 설정 위치 | `~/.gemini/settings.json` | `~/.gemini/antigravity-cli/settings.json` |
| 사용량 | 넉넉했음 | **매우 빡빡함**(5-5절) |

### 5-2. VS Code에서 쓰는 법 — 무엇이 적절한가요?

구글의 코딩 도구는 두 갈래로 나뉩니다. 이 강의는 **VS Code를 표준 편집기**로 쓰므로 선택이 갈립니다.

| 선택지 | 실체 | 이 강의에 적절한가 |
|---|---|---|
| **`agy`를 VS Code 터미널에서 실행** | 터미널 명령. 편집기는 그대로 VS Code | **적절 ○** — Claude Code·Codex와 사용 흐름이 같습니다. **이 강의의 권장 방식** |
| **Antigravity IDE** | **VS Code 포크(별도 프로그램)**. VS Code 확장이 아닙니다 | 부적절 △ — 편집기를 통째로 갈아타야 합니다 |
| **VS Code 확장** | 구글 공식 확장이 없습니다. 마켓플레이스의 서드파티만 존재 | 비권장 × — 공식 지원이 아니라 문제 발생 시 대처가 어렵습니다 |

> **결론** — VS Code를 계속 쓰면서 **터미널에서 `agy`를 실행**하는 방식이 이 강의에 맞습니다. 아래는 모두 이 방식 기준입니다.

### 5-3. 설치와 로그인

**Antigravity CLI는 npm 패키지가 아닙니다.** 설치 스크립트로만 깝니다. Node.js도 필요 없습니다.

```bash
# macOS · Linux
curl -fsSL https://antigravity.google/cli/install.sh | bash
```

```powershell
# Windows · PowerShell
irm https://antigravity.google/cli/install.ps1 | iex
```

```batch
REM Windows · 명령 프롬프트(CMD)
curl -fsSL https://antigravity.google/cli/install.cmd -o install.cmd && install.cmd && del install.cmd
```

설치 스크립트가 OS와 CPU 종류를 알아서 판별해 실행파일을 내려받고, 체크섬을 확인한 뒤 `~/.local/bin/agy`에 넣어 줍니다. 이후 업데이트도 자동입니다.

**실행과 로그인** — 작업 폴더에서 `agy`를 입력하면 브라우저가 열리며 구글 계정 인증을 진행합니다.

```bash
agy
```

**동작 확인** — 한 줄 질문으로 바로 테스트해 볼 수 있습니다.

```bash
agy -p "이 폴더에 뭐가 들어 있는지 요약해줘"
```

> ✅ **체크포인트**: 폴더 내용 요약이 출력되면 성공입니다.

> **기존 Gemini CLI 사용자** — 설정을 옮기는 마법사가 있습니다. 첫 실행 때 가져오기를 수락하면 MCP 서버 등록·허용 명령어·키바인딩이 자동으로 넘어옵니다. 수동으로 하려면 아래 명령을 쓰고 `agy inspect`로 결과를 확인하세요.

```bash
agy plugin import gemini
agy inspect
```

### 5-4. 같은 과제 실습

```text
'오늘의 할 일' 웹앱을 index.html 한 파일로 만들어 줘.
- 할 일 추가/완료체크/삭제
- 브라우저에 저장(localStorage)
- 무료 폰트만 사용하고, 한국어 주석을 충분히 달아 줘
```

구글 도구의 강점(검색 근거)을 살린 추가 요청입니다.

```text
모바일 접근성(a11y) 최신 권고를 검색해서 확인하고,
그 권고에 맞춰 버튼과 입력창에 적절한 라벨을 붙여 줘. 출처 링크도 알려줘.
```

→ 검색 결과를 **근거로 찾아 반영**하는 모습을 강의에서 시연할 수 있습니다.

### 5-5. 무료 한도 주의 (강의 전 확인 필수)

> **"무료로 넉넉하게"는 옛말입니다.** 무료 등급은 **하루 20회 안팎**의 에이전트 요청으로 제한됩니다(5시간 단위로 조금씩 회복). 실습 도중 한도에 걸려 멈추는 사례가 흔합니다. **강의 실습의 기본 도구로는 권장하지 않습니다.** 유료(Pro / AI Ultra)를 쓰거나, 시연·보조용으로만 사용하세요. 유료 등급도 2026년 3월부터 **주 단위 한도**로 바뀌어, 한도를 다 쓰면 최대 일주일간 잠길 수 있습니다.

- **서브에이전트 병렬 실행은 사용량을 아주 빠르게 소모합니다.** 입문 단계에서는 쓰지 마세요.
- `/usage`(또는 `/quota`)로 남은 양을 확인하는 습관이 필요합니다.
- 추가 크레딧 구매도 가능하지만 입문 실습에 필요한 수준은 아닙니다.

### 5-6. 설정과 승인 모드

| 항목 | 위치 / 값 |
|---|---|
| 프로젝트 규칙 파일 | `AGENTS.md` (여러 개로 나누려면 `.agents/rules/*.md`) |
| 설정 | `~/.gemini/antigravity-cli/settings.json` |
| 스킬(나만의 절차) | `.agents/skills/<이름>.md` → `/<이름>`으로 호출 |
| MCP 연결 | 프로젝트 `.agents/mcp_config.json`, 전역 `~/.gemini/config/mcp_config.json` |

**승인 모드(`toolPermission`)** — 설정 파일이나 `/permissions`로 바꿉니다.

| 값 | 동작 | 언제 |
|---|---|---|
| `request-review` | 쓰기·터미널·웹 도구를 쓰기 전에 승인 요청 | **기본값. 초보는 이대로** |
| `strict` | 읽기 외 모든 도구에 승인 요청 | 가장 조심스러울 때 |
| `proceed-in-sandbox` | 샌드박스 안 명령은 자동 실행, 나머지는 승인 요청 | 익숙해진 뒤 |
| `always-proceed` | 아무것도 묻지 않고 실행 | **강의에서는 사용 금지** |

**자주 쓰는 슬래시 명령**

| 명령 | 하는 일 |
|---|---|
| `/usage` (= `/quota`) | 남은 사용량 확인. **실습 중 수시로 확인하세요** |
| `/config` (= `/settings`) | 설정 확인·변경 |
| `/permissions` | 승인 규칙 확인·변경 |
| `/diff` | 바뀐 파일 확인 |
| `/skills` | 사용 가능한 스킬 목록 |
| `/agents` · `/tasks` | 서브에이전트·작업 진행 상황 |
| `/mcp` | MCP 서버 연결 관리 |
| `/clear` (= `/new`) | 새 대화 시작 |
| `/resume` | 이전 대화 이어서 하기 |

---

## 6부. 도구 비교와 공통 원칙

### 6-1. 한눈에 비교 (2026년 8월 기준)

| 항목 | **Claude Code** | **OpenAI Codex** | **Antigravity CLI** |
|---|---|---|---|
| 설치(스크립트) | `claude.ai/install.sh` (3-2절) | `chatgpt.com/codex/install.sh` (4-2절) | `antigravity.google/cli/install.sh` (5-3절) |
| 설치(npm) | `npm i -g @anthropic-ai/claude-code` | `npm i -g @openai/codex` | **npm 없음** |
| Node.js 필요 | 스크립트 설치 시 불필요 | 스크립트 설치 시 불필요 | 불필요 |
| 실행 | `claude` | `codex` | `agy` |
| 기본 모델 | Claude Opus 5 | GPT-5.6 계열 | Gemini 3 계열 |
| 로그인 | Anthropic 계정 | ChatGPT 계정 | 구글 계정 |
| 무료 시작 | 불가(Pro 이상 필요) | 제한적(Plus 권장) | **하루 20회 안팎** |
| 규칙 파일 | `CLAUDE.md` | `AGENTS.md` | `AGENTS.md` |
| VS Code 통합 | 공식 확장 있음 | 공식 확장 있음 | **공식 확장 없음**. 터미널에서 실행 |
| 강점 | 코딩 품질·입문 자료 풍부 | 장시간 자율 작업·생태계·국내 이용자 다수 | 검색 근거 활용·구글 연동 |
| 주의 | 구독 비용 | diff 미리보기 약함 | 무료 한도 매우 빡빡 |

> **초보 추천 동선**: ① **Claude Code**로 감을 잡고(자료가 가장 많습니다) → ② 이미 ChatGPT를 결제 중이면 **Codex**로 옮겨 계속 → ③ 구글 도구는 검색 근거가 필요한 작업에서 보조로 활용.

### 6-2. 어떤 도구든 통하는 원칙 5가지

1. **작게 쪼개서 시킬 것** — "앱 전체"가 아니라 "기능 하나"씩.
2. **조건을 구체적으로 적을 것** — 파일 형식, 디자인, 저장 방식까지 명시.
3. **계획을 먼저 보고 승인할 것** — 실행 전에 무엇을 할지 확인.
4. **항상 실행해서 확인할 것** — AI는 틀릴 수 있습니다.
5. **규칙은 `.md` 파일로 고정할 것** — `CLAUDE.md` / `AGENTS.md`.

### 6-3. 좋은 프롬프트 vs 아쉬운 프롬프트

| 아쉬움 | 좋음 |
|---|---|
| "할 일 앱 만들어 줘" | "할 일을 추가·삭제·완료체크할 수 있는 웹앱을, index.html 한 파일로, 브라우저 저장 기능 포함해서 만들어 줘" |
| "예쁘게 해 줘" | "배경은 흰색, 버튼은 파란색, 모바일에서도 한 줄로 보이게 해 줘" |
| "안 돼" | "추가 버튼을 눌러도 목록에 안 나타나. 콘솔에 무슨 오류가 있는지 확인하고 고쳐 줘" |
| "패키지 깔아 줘" | "`.venv`를 활성화한 상태에서 `pip install requests`를 실행하고, `requirements.txt`도 갱신해 줘" |

---

## 부록 A. 자주 나오는 오류 해결 {#a}

| 증상 | 해결 |
|---|---|
| `command not found: claude` / `codex` / `agy` | 설치가 안 됐거나 PATH 문제입니다. **터미널을 완전히 껐다 켜고** 설치 명령을 다시 실행하세요 |
| `npm: command not found` | Node.js 미설치 → `nodejs.org`에서 LTS 설치. 또는 **설치 스크립트 방식**으로 바꾸세요 |
| Windows에서 `'irm' is not recognized` | CMD 창에서 PowerShell 명령을 쓴 것입니다. PowerShell을 열거나 CMD용 명령을 쓰세요 |
| Windows에서 `'&&' is not a valid statement separator` | 반대로 PowerShell에서 CMD 명령을 쓴 것입니다 |
| `gemini` 실행 시 `HTTP 410` | Gemini CLI가 종료됐습니다. **Antigravity CLI(`agy`)**로 이전하세요(5부) |
| `ModuleNotFoundError` | 가상환경 미활성화. `(.venv)` 표시를 확인한 뒤 `pip install` 재실행 |
| PowerShell에서 활성화 스크립트 차단 | `Set-ExecutionPolicy -Scope CurrentUser RemoteSigned` 실행 후 재시도 |
| 한글이 깨짐 | 파일 인코딩을 **UTF-8**로 저장 |
| 권한/실행 승인 요청이 떴을 때 | 내용을 읽고 **이상 없으면 승인**, 모르겠으면 거절한 뒤 AI에게 물어보세요 |
| 사용량 한도 초과 | Antigravity는 `/usage`로 확인. 무료 등급은 금방 소진되니 유료 도구로 전환하세요 |

> **모든 오류의 1번 해결책** — 오류 메시지를 **그대로 복사해서** AI에게 붙여 넣고 "내 OS는 ○○이야. 원인과 해결 방법을 알려줘"라고 물어보세요. 설치 문제는 AI가 가장 잘 푸는 영역입니다.

---

## 부록 B. 용어 사전 {#b}

| 용어 | 쉬운 설명 |
|---|---|
| **터미널** | 명령어를 글자로 입력하는 까만 창. VS Code 아래쪽에 붙어 있습니다 |
| **CLI** | Command Line Interface. 터미널에서 글자로 조작하는 프로그램 |
| **에이전트** | 추천만 하는 게 아니라 **파일을 직접 만들고 명령까지 실행**하는 AI |
| **PATH** | 컴퓨터가 프로그램을 찾아다니는 경로 목록. 여기 없으면 "command not found" |
| **패키지** | 남이 만들어 둔 기능 묶음. 가져다 쓰면 됩니다 |
| **npm** | Node.js용 패키지 설치 도구 |
| **pip** | 파이썬용 패키지 설치 도구 |
| **가상환경(.venv)** | 프로젝트 전용 서랍. 패키지가 섞이지 않게 격리합니다 |
| **curl / irm** | 인터넷에서 파일을 받아 오는 명령. 설치 스크립트를 내려받아 실행할 때 씁니다 |
| **diff** | 무엇이 바뀌었는지 보여주는 비교 화면 |
| **프롬프트** | AI에게 주는 지시문 |
| **할루시네이션** | AI가 그럴듯하지만 틀린 내용을 지어내는 현상 |
| **localStorage** | 브라우저가 데이터를 기억해 두는 저장소. 새로고침해도 남습니다 |
| **MCP** | AI를 깃허브·DB 같은 외부 도구에 연결하는 표준 규격(심화 가이드 참고) |

---

## 부록 C. Codex만으로 진행하기 {#c}

이 문서는 Claude Code를 기준으로 썼지만, **OpenAI Codex만으로도 전 과정을 그대로 진행할 수 있습니다.** ChatGPT 구독자가 많다면 이 절만 따라가면 됩니다. 바꿀 것은 **명령어·파일명·승인 방식 세 가지**뿐입니다.

### C-1. 개념 대응표

| 개념 | Claude Code | OpenAI Codex |
|---|---|---|
| 설치(스크립트) | `claude.ai/install.sh` (3-2절) | `chatgpt.com/codex/install.sh` (4-2절) |
| 설치(npm) | `npm i -g @anthropic-ai/claude-code` | `npm i -g @openai/codex` |
| 실행 | `claude` | `codex` |
| 로그인 | Anthropic 계정 | ChatGPT 계정 |
| 프로젝트 규칙 파일 | `CLAUDE.md` | **`AGENTS.md`** (내용은 그대로) |
| 규칙 파일 자동 생성 | `/init` | `/init` |
| 계획 먼저 보기 | `Shift+Tab` 플랜 모드 | `/plan` |
| 실행 | 승인 프롬프트에 동의 | `/exec` |
| 검토 | `/review` | `/review` |
| 바뀐 파일 확인 | 인라인 diff | `/diff` |
| 모델 변경 | `/model` | `/model` |
| 현재 상태 확인 | `/context` | `/status` |
| 권한 | 매번 승인 프롬프트 | 샌드박스 모드로 미리 지정 |
| VS Code 확장 | Claude Code for VS Code | OpenAI Codex |

### C-2. 설치부터 첫 실습까지

**① 공통 준비는 동일합니다** — VS Code·터미널·Node.js·파이썬·작업 폴더(1부), 가상환경(2부). 여기까지는 도구와 무관하게 같습니다.

**② 설치와 로그인**

```bash
curl -fsSL https://chatgpt.com/codex/install.sh | sh
cd my-first-vibe
codex
```

**③ 권한(샌드박스) 정하기** — 실습에서는 `workspace-write`가 적절합니다. 작업 폴더 안의 파일만 고칠 수 있습니다.

| 모드 | 범위 | 언제 |
|---|---|---|
| `read-only` | 읽기만 | 코드 구경·설명 요청 |
| `workspace-write` | 작업 폴더 안 쓰기 | **실습 기본값** |
| `danger-full-access` / `--yolo` | 전체 허용 | 격리된 환경에서만. **강의에서는 사용 금지** |

**④ 규칙 파일 만들기** — `AGENTS.md`를 작업 폴더 최상단에 저장합니다. 3-7절의 `CLAUDE.md` 내용을 그대로 옮기면 됩니다.

```markdown
# 프로젝트 규칙
- 모든 설명과 주석은 한국어로 작성한다.
- 초보자가 읽기 쉽게 변수명은 명확하게 짓는다.
- 외부 라이브러리는 꼭 필요할 때만 쓴다.
- 파이썬 패키지는 .venv 가상환경에 pip로 설치한다. 설치 후 requirements.txt를 갱신한다.
- 파일을 고치기 전에 무엇을 바꿀지 먼저 알려준다.
```

**⑤ 할 일 웹앱 만들기** — 3-4절 프롬프트를 그대로 쓰되, 계획·실행을 끊어서 지시합니다.

```text
/plan
초보자도 이해하기 쉽게, 할 일을 추가·완료체크·삭제할 수 있는 '오늘의 할 일' 웹앱을 만들어 줘.
조건:
1) 설치 없이 브라우저로 바로 열리도록 index.html 파일 하나로 만들 것
2) 디자인은 깔끔하고 모바일에서도 보기 좋게
3) 할 일 목록은 새로고침해도 남아 있도록 브라우저에 저장
```

```text
/exec
```

```text
/diff
```

**⑥ 파이썬 프로그램 만들기** — 3-6절과 동일합니다. 가상환경을 켠 뒤 진행하세요.

```text
/plan
글자 수·단어 수·문장 수를 세는 파이썬 프로그램을 wordcount.py로 만들어 줘.
표준 라이브러리만 쓰고, 한국어 주석을 충분히 달아 줘. 만든 뒤 실행해서 결과도 보여줘.
```

### C-3. Codex를 쓸 때 특히 주의할 점

- **바뀐 파일을 꼭 눈으로 확인하세요** — diff 미리보기가 Claude Code보다 약합니다. 작업 후 `/diff`를 습관화하세요.
- **`--yolo`·`danger-full-access` 금지** — 승인과 샌드박스를 모두 끕니다. 강의 환경에서는 절대 쓰지 마세요.
- **웹 검색이 환경에 따라 약합니다** — 근거 검색이 필요한 작업은 결과를 반드시 링크로 확인하세요. 부족하면 검색 MCP(Tavily·Brave 등)를 붙이는 방법도 있습니다(심화 가이드 참고).
- **명령·설정이 자주 바뀝니다** — `codex` 안에서 `/help`로 현재 버전의 명령 목록을 먼저 확인하세요.

> **핵심** — 도구가 달라져도 바이브 코딩의 사이클은 같습니다. **말로 요청 → 계획 확인 → 실행 → 결과 검수 → 규칙 파일로 고정.** 이 흐름만 몸에 익히면 어느 도구로 옮겨도 30분이면 적응할 수 있습니다.

---

## 참고문헌

1. 조태호, 『혼자 공부하는 바이브코딩 with 클로드코드』 공식 GitHub. https://github.com/taehojo/vibecoding
2. Claude Code 설치·설정 공식 문서. https://code.claude.com/docs/en/setup
3. Claude Code VS Code 확장 공식 문서. https://code.claude.com/docs/en/vs-code
4. Anthropic, 모델 개요(Claude Opus 5 · Sonnet 5 · Haiku 4.5). https://platform.claude.com/docs/en/about-claude/models/overview
5. OpenAI Codex 공식 저장소(설치 명령). https://github.com/openai/codex
6. OpenAI Codex 모델 목록·종료 일정. https://learn.chatgpt.com/docs/models
7. OpenAI Codex 변경 이력. https://learn.chatgpt.com/docs/changelog
8. Google, Gemini CLI → Antigravity CLI 이전 안내(2026.6.18). https://antigravity.google/docs/gcli-migration
9. Google Antigravity CLI 설치·인증 공식 문서. https://antigravity.google/docs/cli/install/
10. Google Antigravity CLI 설정(settings.json) 문서. https://antigravity.google/docs/cli/settings/
11. 파이썬 공식 문서 — `venv` 가상환경. https://docs.python.org/ko/3/library/venv.html
12. 파이썬 공식 가이드 — pip로 패키지 설치하기. https://packaging.python.org/ko/latest/tutorials/installing-packages/
13. Visual Studio Code 공식 다운로드. https://code.visualstudio.com
14. Node.js 공식 다운로드(LTS). https://nodejs.org

> 도구 버전·모델·요금·전환 일정은 2026년 8월 기준이며 변경이 잦습니다. 위 1차 출처에서 최신 정보를 확인하세요.
